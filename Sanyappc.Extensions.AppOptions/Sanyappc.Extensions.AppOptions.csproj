<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Authors>Alexander Mokrov</Authors>
    <Copyright>Copyright (c) 2026 Alexander Mokrov</Copyright>
    <Description>A .NET library for typed, validated application configuration. Integrates with IHostApplicationBuilder to bind options from a configuration section, register them in DI, and validate them — both eagerly at startup and lazily on first use — using Data Annotations and optional custom validation logic.</Description>
    <PackageTags>options configuration validation dataannotations hosting appsettings startup typed</PackageTags>
    <PackageProjectUrl>https://github.com/sanyappc/Sanyappc.Extensions.AppOptions</PackageProjectUrl>
    <RepositoryUrl>https://github.com/sanyappc/Sanyappc.Extensions.AppOptions</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.*" />
    <PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" Version="10.0.*" />
    <PackageReference Include="Microsoft.Extensions.Options.DataAnnotations" Version="10.0.*" />
  </ItemGroup>

</Project>
